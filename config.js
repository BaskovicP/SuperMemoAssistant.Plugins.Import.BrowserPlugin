'use strict';

const app = {
  id: 'supermemoassistant.plugins.import.browserextension',
  name: 'SuperMemo Connector',
  isLogEnabled: true,
  isVerboseLogEnabled: true
};

app.locale = {
  current: 'Import current Tab into SuperMemo',
  all: 'Import all Tabs into SuperMemo',
  call: 'Import all Tabs into SuperMemo (Current window)'
};
